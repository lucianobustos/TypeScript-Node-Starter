include header
style(type="text/css").
    body {
        color:#4267b2;
        font-family: "San Francisco", "Segoe UI", "FreeSans", "Roboto", sans-serif;
    }
h1 #{user.profile.name}
img(src=user.profile.picture, class='profile', width='100', height='100', alt=((user.profile.name || user.email || user.id)))
p 💌 Email:  
    a(href='mailto:' + user.email) !{user.email}
include footer